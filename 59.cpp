#include <iostream>
#include <algorithm>

using namespace std;

class Solution {
public:
    bool isValid(string s) {
        
    }
};

int main(){
    string s;
    cout << "Enter the string : ";
    cin>>;
    Solution obj;
    cout<<obj.isValid(s);
    return 0;
}